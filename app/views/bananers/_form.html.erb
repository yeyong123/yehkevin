<%= form_for(@bananer, html: {multipart: true}) do |f| %>
  <% if @bananer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@bananer.errors.count, "error") %> prohibited this bananer from being saved:</h2>

      <ul>
      <% @bananer.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :image %><br />
    <%= f.file_field :image %>
  </div>
  <div class="field">
    <%= f.label :describe %><br />
    <%= f.text_area :describe %>
  </div>
  <div class="field">
    <%= f.label :button %><br />
    <%= f.text_field :button %>
  </div>
  <div class="field">
    <%= f.label :url %><br />
    <%= f.text_field :url %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
